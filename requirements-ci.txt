# Lightweight dependency set for the unit-test workflow.
# Full research runs still require requirements.txt (including torch/transformers/pymoo).
numpy>=1.23
scipy>=1.11,<1.13
scikit-learn>=1.3.0
networkx>=3.0
rouge-score>=0.1.2
# Pin, not floor -- see requirements.txt / docs/research/COMPUTE_ENVIRONMENT.md.
nltk==3.10.0
regex>=2023.6.3
pyyaml>=6.0
tqdm>=4.64
pytest>=7.4
# LexRank/TextRank baseline tests need this importable in CI (lightweight,
# no torch-scale cost).
sumy==0.12.0
