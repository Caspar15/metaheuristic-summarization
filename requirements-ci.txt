# Dependency set for the unit-test workflow.  The workflow installs the pinned
# CPU-only torch wheel first; listing it here keeps this file the complete CI
# contract.  Full research runs still require requirements.txt (including
# pymoo and the remaining runtime dependencies).
numpy>=1.23
scipy>=1.11,<1.13
scikit-learn>=1.3.0
networkx>=3.0
rouge-score>=0.1.2
# Pin, not floor -- see requirements.txt / docs/research/COMPUTE_ENVIRONMENT.md.
nltk==3.10.0
regex>=2023.6.3
pyyaml>=6.0
tqdm>=4.64
pytest>=7.4
# LexRank/TextRank baseline tests need this importable in CI (lightweight,
# no torch-scale cost).
sumy==0.12.0
# Frozen audit-runner contract tests import these modules during collection.
# They are exact pins from requirements.txt; CI never downloads Stanza models
# or executes the external Perl ROUGE toolkit.
psutil==7.0.0
stanza==1.10.1
pyrouge==0.1.3
# Encoder representation, cache, PacSum-SBERT, and integration contract tests
# use small in-memory tensors and mocked checkpoints; they never download a
# model, but torch and transformers must be importable during test collection.
torch==2.8.0
transformers==4.56.0
