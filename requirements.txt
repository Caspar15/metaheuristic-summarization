# ---------------------------------------------------------------------------
# Core numerical / data
# ---------------------------------------------------------------------------
numpy>=1.23
# scipy 1.10.1 不支援 Python 3.12，放寬版本以相容 3.12
scipy>=1.11,<1.13
pandas>=1.5
scikit-learn>=1.3.0
networkx>=3.0

# ---------------------------------------------------------------------------
# Optimisation (NSGA-II)
# ---------------------------------------------------------------------------
# 版本鎖定：BitflipMutation 的 prob 語義（per-individual 1.0，per-gene 1/n_var）
# 是依 0.6.1.1 驗證的，升版前請重新確認。
pymoo==0.6.1.1

# ---------------------------------------------------------------------------
# NLP / evaluation
# ---------------------------------------------------------------------------
rouge-score>=0.1.2
# 精確 pin，不是下限：離線 NLTK_DATA 快取內容隨版本改變（3.8.x 讀 punkt
# pickle，3.9+ 的 switch_punkt shim 一律改讀 punkt_tab），升版前必須重跑
# docs/research/COMPUTE_ENVIRONMENT.md 的三個實測，不能假設沿用快取。
nltk==3.10.0
regex>=2023.6.3
datasets>=2.14.0
# LexRank/TextRank baseline（Phase 2）；Apache-2.0，見 NOTICE。
sumy==0.12.0

# ---------------------------------------------------------------------------
# Encoders (Stage-1 semantic route)
# ---------------------------------------------------------------------------
torch>=2.0
transformers>=4.38
# 建議改用 sentence-similarity checkpoint 取代 raw BERT centroid，見 docs
# sentence-transformers>=2.2

# ---------------------------------------------------------------------------
# CLI / IO
# ---------------------------------------------------------------------------
pyyaml>=6.0
tqdm>=4.64
click>=8.1
ujson>=5.7; platform_system != 'Windows'
orjson>=3.9; platform_system == 'Windows'

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest>=7.4

# ---------------------------------------------------------------------------
# Demo web app only — NOT required to reproduce any research result.
# 見 requirements-demo.txt
# ---------------------------------------------------------------------------
